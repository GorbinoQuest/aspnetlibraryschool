Add Search bar in group add user thingy, so the amount of users gets filtered based on their name

Add search bar inside book catalogue so the books get filtered down as well.

Add history lookup based on user for the librarian

Group duplicate book entries based on their name, author, release date, etc. inside book Catalogue. Show them by using the + sign.







When the book QR code is scanned, a link is given to a controller action which checks if the book is available. If it is, redirect user to a view thats responsible for book giving, else redirect to book returning.
