Add Search bar in group add user thingy, so the amount of users gets filtered


make it so through UserManagement/GenerateLoginDetails/Id and GroupManagement/Details/GenerateLoginDetails, admin user can send DefaultPassword to user e-mail. For now, have it generate a .CSV file with user login details (email, generatedPassword). Once an user changes their password inside UserManagement, remove the Temporary password field and disable the GenerateLoginDetails, and don't include their password or entry entirely inside GroupManagement/Details/GenerateLoginDetails






When the book QR code is scanned, a link is given to a controller action which checks if the book is available. If it is, redirect user to a view thats responsible for book giving, else redirect to book returning.
